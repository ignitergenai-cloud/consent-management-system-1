apiVersion: apps/v1
kind: Deployment
metadata:
  name: incident-detector
  namespace: cms
  labels:
    app.kubernetes.io/name: incident-detector
    app.kubernetes.io/part-of: consent-management-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: incident-detector
  template:
    metadata:
      labels:
        app.kubernetes.io/name: incident-detector
    spec:
      containers:
        - name: incident-detector
          image: cms/incident-detector:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8003
          envFrom:
            - configMapRef:
                name: aws-config
            - configMapRef:
                name: service-config
            - secretRef:
                name: aws-credentials
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: incident-detector
  namespace: cms
  labels:
    app.kubernetes.io/name: incident-detector
spec:
  type: ClusterIP
  ports:
    - port: 8003
      targetPort: 8003
      protocol: TCP
  selector:
    app.kubernetes.io/name: incident-detector
