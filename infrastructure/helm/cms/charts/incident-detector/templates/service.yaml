apiVersion: v1
kind: Service
metadata:
  name: {{ include "incident-detector.fullname" . }}
  labels:
    {{- include "incident-detector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "incident-detector.selectorLabels" . | nindent 4 }}
